// reusing function 

function calculateRectangleArea(width, height) {
    return width * height;
}

// rect 1
const rectangleWidth = 5;
const rectangleheight = 10;
const area1 = calculateRectangleArea(rectangleWidth, rectangleheight);
console.log(`Area of rectange: ${area1}`)

// rect 2
const rectangle2width = 9;
const rectange3height = 10;
const area2 = calculateRectangleArea(rectangle2width, rectange3height);
console.log(`Area of rectangel: ${ area2
}`)
